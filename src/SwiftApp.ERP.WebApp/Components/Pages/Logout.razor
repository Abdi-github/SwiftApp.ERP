@page "/logout"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to the server-side logout endpoint that clears the cookie
        Navigation.NavigateTo("/account/logout", forceLoad: true);
    }
}
